import React from 'react';
import GameBoard from '@/components/Game/GameBoard';

export default function Home() {
    return (
        <main className="py-6">
            <GameBoard />
        </main>
    );
}